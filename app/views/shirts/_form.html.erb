<%= form_with(model: shirt) do |form| %>
  <% if shirt.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(shirt.errors.count, "error") %> prohibited this shirt from being saved:</h2>

      <ul>
        <% shirt.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :size, style: "display: block" %>
    <%= form.text_field :size %>
  </div>

  <div>
    <%= form.label :brand, style: "display: block" %>
    <%= form.text_field :brand %>
  </div>

  <div>
    <%= form.label :color, style: "display: block" %>
    <%= form.text_field :color %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
